let message1: string = `hello world`;

//加注解不加都行  可以推导出就可以不加
const name = 'code';
const age = 20;
const height = 1.99;
let message2 = `${name} ${age} ${height}`;
console.log(message1, message2);

export {};
